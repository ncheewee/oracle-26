{
  "name": "oracle-26",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit-data.mjs",
    "collect": "node scripts/collect-worldcup.mjs",
    "model": "node scripts/train-model.mjs",
    "simulate": "node scripts/simulate-tournament.mjs",
    "odds": "node scripts/collect-market-odds.mjs",
    "refresh": "pnpm run audit && pnpm run collect && pnpm run model && pnpm run simulate && pnpm run odds",
    "serve": "python3 -m http.server 4173",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}
